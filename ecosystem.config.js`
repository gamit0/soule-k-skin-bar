module.exports = {
  apps: [
    {
      name: "soule-k-skin-bar",
      script: "npm",
      args: "start",
      cwd: process.cwd(),
      instances: "max", // Aprovecha todos los núcleos del CPU
      exec_mode: "cluster",
      env: {
        NODE_ENV: "production",
      },
      error_file: "./logs/err.log",
      out_file: "./logs/out.log",
      log_date_format: "YYYY-MM-DD HH:mm Z",
      restart_delay: 3000,
      max_memory_restart: "1G",
    },
  ],
};
